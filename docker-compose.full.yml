name: thoth-chat

services:
  app:
    image: ${THOTH_CHAT_IMAGE:-ghcr.io/thoth-ai/thoth-chat:latest}
    container_name: thoth-chat-app
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Full compose connects to bundled services on the compose network.
      # These environment variables override database and Redis values in config.yaml.
      POSTGRES_DSN: "postgres://thoth_chat:thoth_chat_2026@postgres:5432/thoth_chat?sslmode=disable&TimeZone=Asia/Shanghai"
      REDIS_ADDR: "redis:6379"
      REDIS_USERNAME: ""
      REDIS_PASSWORD: "thoth_chat_2026"
    ports:
      # Optional. Localhost-only by default. Put a reverse proxy in front for public access.
      - "127.0.0.1:8080:8080"
    volumes:
      - app_storage:/app/storage
      - app_data:/app/data
      # The image runs from /app and reads /app/config.yaml automatically.
      - ./config.yaml:/app/config.yaml:ro
    networks:
      - thoth-chat

  postgres:
    image: pgvector/pgvector:pg16
    container_name: thoth-chat-postgres
    environment:
      POSTGRES_USER: thoth_chat
      POSTGRES_PASSWORD: thoth_chat_2026
      POSTGRES_DB: thoth_chat
    ports:
      # Optional. Localhost-only for local administration; the app uses the internal Docker network.
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - thoth-chat

  redis:
    image: redis:7
    container_name: thoth-chat-redis
    command: redis-server --requirepass thoth_chat_2026
    ports:
      # Optional. Localhost-only for local administration; the app uses the internal Docker network.
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "thoth_chat_2026", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - thoth-chat

volumes:
  app_storage:
    name: thoth-chat-app-storage
  app_data:
    name: thoth-chat-app-data
  postgres_data:
    name: thoth-chat-postgres-data
  redis_data:
    name: thoth-chat-redis-data

networks:
  thoth-chat:
    name: thoth-chat-network
