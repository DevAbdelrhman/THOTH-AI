{
  "name": "@thoth/chat-monorepo",
  "version": "0.3.6",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "api:check": "pnpm --filter @thoth/api-contract check",
    "api:generate": "pnpm --filter @thoth/api-contract generate",
    "build": "turbo run build",
    "check": "turbo run check",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=@thoth/api",
    "dev:web": "turbo run dev --filter=@thoth/web",
    "test": "turbo run test",
    "verify": "turbo run check test build"
  },
  "devDependencies": {
    "turbo": "2.10.5"
  },
  "pnpm": {
    "overrides": {
      "dompurify": "3.4.13"
    }
  }
}
