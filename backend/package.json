{
  "name": "@thoth/api",
  "version": "0.3.6",
  "private": true,
  "scripts": {
    "build": "make build",
    "check": "make check-version && go vet ./...",
    "clean": "rm -rf ../.cache/thoth-chat",
    "dev": "make run",
    "test": "make test"
  }
}
